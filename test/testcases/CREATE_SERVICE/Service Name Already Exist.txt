*** Settings ***
Suite Setup       Suite Setup
Force Tags        create service
Test Template     Create Service Template
Resource          ../../resources/keywords.txt

*** Variables ***
${working_dir}    ${_FILE_WORKING_DIR}${/}FileCommand${/}TestWindowsService

*** Test Cases ***    SERVICE NAME              SERVICE PATH      SERVICE TYPE    DISPLAYED NAME 1             DISPLAYED NAME 2             RETURN CODE    RETURN STATUS
Service Name Already Exist
                      UC4.TestWindowsService    DhmService.exe    filesys         UC4.TestWindowsService 01    UC4.TestWindowsService 02    1              ${ENDED_NOT_OK - aborted}

*** Keywords ***
Suite Setup
    Init AE And Package
    Init Win File Server

Create Service Template
    [Arguments]    ${serviceName}    ${servicePath}    ${serviceType}    ${serviceDisplayedName1}    ${serviceDisplayedName2}    ${returnCode}
    ...    ${returnStatus}
    Init Action    ${ACTION_DELETE_SERVICE}
    Action Set    ${PROMPTSET_SERVICE_NAME}    ${serviceName}
    Action Execute
    Init Action    ${ACTION_CREATE_SERVICE}
    Action Set    ${PROMPTSET_SERVICE_NAME}    ${serviceName}
    Action Set    ${PROMPTSET_SERVICE_PATH}    ${working_dir}${/}${servicePath}
    Action Set    ${PROMPTSET_SERVICE_TYPE}    ${serviceType}
    Action Set    ${PROMPTSET_SERVICE_DISPLAY_NAME}    ${serviceDisplayedName1}
    Action Execute
    Init Action    ${ACTION_CREATE_SERVICE}
    Action Set    ${PROMPTSET_SERVICE_NAME}    ${serviceName}
    Action Set    ${PROMPTSET_SERVICE_PATH}    ${working_dir}${/}${servicePath}
    Action Set    ${PROMPTSET_SERVICE_TYPE}    ${serviceType}
    Action Set    ${PROMPTSET_SERVICE_DISPLAY_NAME}    ${serviceDisplayedName2}
    Action Execute
    Action Report Should Be Found
    Action Return Code Should Be    ${returnCode}
    Action Return Status Should Be    ${returnStatus}
    Action Report Should Contain    START AT
    #    Action Report Should Contain    PARAMETER VALIDATION START
    #    Action Report Should Contain    EXECUTION START
    #    Action Report Should Contain    ABEND AT
    #    Action Report Should Contain    RET-CODE
