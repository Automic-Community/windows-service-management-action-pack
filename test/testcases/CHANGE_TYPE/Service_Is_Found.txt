*** Settings ***
Suite Setup       Suite Setup
Force Tags        change type
Test Template     Change Type Template
Resource          ../../resources/keywords.txt

*** Variables ***
${working_dir}    ${_FILE_WORKING_DIR}\\FileCommand\\WindowsServiceManager\\ServiceManager\\windows\\x64

*** Test Cases ***    SERVICE NAME                                                  SERVICE PATH       SERVICE TYPE 1    DISPLAYED NAME     SERVICE TYPE 2     RETURN CODE    RETURN STATUS
Change To Own Type 1
                      UC4.TestService                                               UCYBSMgr.exe       own               UC4.TestService    own                0              ${ENDED_OK - ended normally}

Change To Own Type 2
                      UC4.TestService                                               UCYBSMgr.exe       share             UC4.TestService    own                0              ${ENDED_OK - ended normally}

Change To Share Type 1
                      UC4.TestService                                               UCYBSMgr.exe       own               UC4.TestService    share              0              ${ENDED_OK - ended normally}

Change To Share Type 2
                      UC4.TestService                                               UCYBSMgr.exe       share             UC4.TestService    share              0              ${ENDED_OK - ended normally}
                      #Change To Invalid Level
                      #                                                             UC4.TestService    UCYBSMgr.exe      filesys            UC4.TestService    INVALID        1                               ${ENDED_NOT_OK - aborted}
                      #Cannot create service with type is "kernel" and "filesys"

*** Keywords ***
Suite Setup
    Init AE And Package
    Init Win File Server

Change Type Template
    [Arguments]    ${serviceName}    ${servicePath}    ${serviceType1}    ${serviceDisplayedName}    ${serviceType2}    ${returnCode}
    ...    ${returnStatus}
    Init Action    ${ACTION_STOP_SERVICE}
    Action Set    ${PROMPTSET_SERVICE_NAME}    ${serviceName}
    Action Execute
    Init Action    ${ACTION_DELETE_SERVICE}
    Action Set    ${PROMPTSET_SERVICE_NAME}    ${serviceName}
    Action Execute
    Init Action    ${ACTION_CREATE_SERVICE}
    Action Set    ${PROMPTSET_SERVICE_NAME}    ${serviceName}
    Action Set    ${PROMPTSET_SERVICE_PATH}    ${working_dir}${/}${servicePath}
    Action Set    ${PROMPTSET_SERVICE_TYPE}    ${serviceType1}
    Action Set    ${PROMPTSET_SERVICE_DISPLAY_NAME}    ${serviceDisplayedName}
    Action Execute    #the step CREATE SERVICE is ENDED_OK but in the report: "echo UC4.TestService already exists. Cannot create."
    Init Action    ${ACTION_CHANGE_TYPE}
    Action Set    ${PROMPTSET_SERVICE_NAME}    ${serviceName}
    Action Set    ${PROMPTSET_SERVICE_TYPE}    ${serviceType2}    #own | share
    Action Execute
    Action Report Should Be Found
    Action Return Code Should Be    ${returnCode}
    Action Return Status Should Be    ${returnStatus}
    Action Report Should Contain    START AT
    #    Action Report Should Contain    PARAMETER VALIDATION START
    #    Action Report Should Contain    EXECUTION START
    #    Action Report Should Contain    ABEND AT
    #    Action Report Should Contain    RET-CODE
