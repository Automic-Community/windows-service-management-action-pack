*** Settings ***
Suite Setup       Suite Setup
Force Tags        change path
Test Template     Change Path Template
Resource          ../../resources/keywords.txt

*** Variables ***
${working_dir}    ${_FILE_WORKING_DIR}${/}FileCommand

*** Test Cases ***    SERVICE NAME       SERVICE PATH 1                                                                   SERVICE TYPE    DISPLAYED NAME     SERVICE PATH 2    RETURN CODE    RETURN STATUS
Normal Path           UC4.TestService    ${/}WindowsServiceManager${/}ServiceManager${/}windows${/}x64${/}UCYBSMgr.exe    own             UC4.TestService    PathChange        0              ${ENDED_OK - ended normally}

*** Keywords ***
Suite Setup
    Init AE And Package
    Init Win File Server

Change Path Template
    [Arguments]    ${serviceName}    ${servicePath1}    ${serviceType}    ${serviceDisplayedName}    ${servicePath2}    ${returnCode}
    ...    ${returnStatus}
    Init Action    ${ACTION_STOP_SERVICE}
    Action Set    ${PROMPTSET_SERVICE_NAME}    ${serviceName}
    Action Execute
    Init Action    ${ACTION_DELETE_SERVICE}
    Action Set    ${PROMPTSET_SERVICE_NAME}    ${serviceName}
    Action Execute
    Init Action    ${ACTION_CREATE_SERVICE}
    Action Set    ${PROMPTSET_SERVICE_NAME}    ${serviceName}
    Action Set    ${PROMPTSET_SERVICE_PATH}    ${working_dir}${/}${servicePath1}
    Action Set    ${PROMPTSET_SERVICE_TYPE}    ${serviceType}
    Action Set    ${PROMPTSET_SERVICE_DISPLAY_NAME}    ${serviceDisplayedName}
    Action Execute
    Init Action    ${ACTION_CHANGE_PATH}
    Action Set    ${PROMPTSET_SERVICE_NAME}    ${serviceName}
    Action Set    ${PROMPTSET_SERVICE_PATH}    ${servicePath2}
    Action Execute
    Action Report Should Be Found
    Action Return Code Should Be    ${returnCode}
    Action Return Status Should Be    ${returnStatus}
    Action Report Should Contain    START AT
    #    Action Report Should Contain    PARAMETER VALIDATION START
    #    Action Report Should Contain    EXECUTION START
    #    Action Report Should Contain    ABEND AT
    #    Action Report Should Contain    RET-CODE
